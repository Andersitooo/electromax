# app/Middleware

Contiene funciones relacionadas con seguridad transversal.

Archivo principal:

- `security.php`: sesión, roles, permisos, CSRF, redirecciones y subida segura de archivos.

La ruta antigua `seguridad.php` se conserva como adaptador.
