# database/migrations

Contiene migraciones del proyecto.

Una migración es un cambio incremental de base de datos, por ejemplo:

```text
agregar una columna
crear una tabla nueva
crear un índice
ajustar una relación
```

Las migraciones deben ejecutarse con cuidado y, de preferencia, una sola vez.
