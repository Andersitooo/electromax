# Carpeta public

Será el webroot futuro del proyecto.

En Fase 2 no se activa como webroot porque XAMPP actualmente ejecuta el proyecto desde la raíz `electro2/`.

Activar `public/` como raíz web se hará solo cuando existan adaptadores y rutas seguras.
